import { ProblemSolution } from './types';

export const solutions: ProblemSolution[] = [
  {
    id: 9001,
    description: `- Shorten long URLs to compact aliases (e.g., 7-character base62 codes)\\n- Redirect short URLs to original destinations with minimal latency\\n- Support custom aliases, expiration, and analytics\\n- Handle 100M+ URLs with 10:1 read-to-write ratio\\n`,
    examples: `Exercise 1: Implement Base62 Encoding\\nWrite a service that takes a numeric ID and converts it to a base62 string (a-z, A-Z, 0-9). Then implement the reverse decoding. Ensure the output is always exactly 7 characters by zero-padding.\\n\\nExercise 2: Design a Key Range Pre-generation Service\\nBuild a service that pre-generates batches of unique 7-character keys and stores them in a database. Worker nodes request a batch of unused keys, mark them as allocated, and use them for new URL mappings without coordination.`,
    intuition: `A URL shortener is fundamentally a distributed hash map that converts long URLs into short keys and retrieves them with single-digit millisecond latency. The core challenge is generating globally unique short codes at scale without coordination bottlenecks.`,
    approach: `A stateless API layer sits behind a load balancer, backed by a distributed key-value store for URL mappings. A separate ID generation service produces unique short codes. A cache layer (Redis) handles hot URLs to reduce database reads.\\n\\n\\n- **ID Generator**: Base62 encoding of auto-increment IDs or pre-generated random keys stored in a key range table\\n- **Database**: NoSQL store (DynamoDB) mapping short code to long URL, creation time, and expiry\\n- **Cache**: LRU cache for frequently accessed URLs\\n- **Analytics Service**: Async event pipeline tracking clicks, referrers, and geolocation\\n`,
    code: `# System Design: Design a URL Shortener (TinyURL)\\n#\\n# Key Components:\\n# - ID Generator: Base62 encoding of auto-increment IDs or pre-generated random keys stored in a key range table\\n# - Database: NoSQL store (DynamoDB) mapping short code to long URL, creation time, and expiry\\n# - Cache: LRU cache for frequently accessed URLs\\n# - Analytics Service: Async event pipeline tracking clicks, referrers, and geolocation\\n#\\n# Architecture Notes:\\n# A stateless API layer sits behind a load balancer, backed by a distributed key-value store for URL m\\n`,
    explanation: `Horizontally scale the API tier. Partition the database by hash of short code. Use consistent hashing for cache distribution. Pre-generate key ranges to avoid collisions without coordination.\\n\\n\\n- Sequential IDs are predictable but simple; random codes need collision checks but are secure\\n- 301 (permanent) redirects reduce server load but lose analytics; 302 (temporary) keeps tracking intact`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Base62 Encoding: Write a service that takes a numeric ID and converts it to a base62 string (a-z, A-Z, 0-9). Then implement the reverse decoding. Ensure the output is always exactly 7 charac", "Design a Key Range Pre-generation Service: Build a service that pre-generates batches of unique 7-character keys and stores them in a database. Worker nodes request a batch of unused keys, mark them a", "Problem: Multiple servers generate the same short code simultaneously, causing URL overwrites. Solution: Use a key pre-generation service (KGS) that allocates unique key ranges to each server. Alterna", "Problem: Database reads become a bottleneck as popular URLs get millions of redirects per second. Solution: Place a multi-tier caching layer (local in-memory + distributed Redis) in front of the datab"],
  },
  {
    id: 9002,
    description: `- Limit requests per user/IP/API-key within configurable time windows\\n- Support multiple algorithms: fixed window, sliding window, token bucket, leaky bucket\\n- Return appropriate HTTP 429 responses with retry-after headers\\n- Operate in a distributed environment with minimal latency overhead\\n`,
    examples: `Exercise 1: Implement a Sliding Window Rate Limiter\\nBuild a rate limiter that uses the sliding window log algorithm. Track timestamps of each request in a sorted set. For each incoming request, remove entries outside the window, count remaining entries, and accept or reject.\\n\\nExercise 2: Build a Token Bucket Implementation\\nCreate a token bucket rate limiter where tokens refill at a fixed rate. Each request consumes one token. If the bucket is empty, reject the request. Support configurable bucket size and refill rate per user.`,
    intuition: `A rate limiter is a gatekeeper that counts requests in time windows and rejects excess traffic to protect backend services from overload. The fundamental tension is between precision (exact counts) and performance (minimal latency overhead per request).`,
    approach: `A rate limiter middleware intercepts requests before they reach the application. It checks counters stored in a fast in-memory store (Redis). Configuration rules are stored in a database and cached locally. The middleware returns pass/reject decisions in under 1ms.\\n\\n\\n- **Rules Engine**: Configurable rules per endpoint, user tier, or API key stored in a config DB\\n- **Counter Store**: Redis with atomic INCR and TTL operations for tracking request counts\\n- **Middleware**: Lightweight interceptor deployed as a sidecar or library in the API gateway\\n- **Response Handler**: Returns 429 status with X-RateLimit-Remaining and Retry-After headers\\n`,
    code: `# System Design: Design a Rate Limiter\\n#\\n# Key Components:\\n# - Rules Engine: Configurable rules per endpoint, user tier, or API key stored in a config DB\\n# - Counter Store: Redis with atomic INCR and TTL operations for tracking request counts\\n# - Middleware: Lightweight interceptor deployed as a sidecar or library in the API gateway\\n# - Response Handler: Returns 429 status with X-RateLimit-Remaining and Retry-After headers\\n#\\n# Architecture Notes:\\n# A rate limiter middleware intercepts requests before they reach the application. It checks counters \\n`,
    explanation: `Use Redis Cluster for partitioned counter storage. Deploy rate limiter logic at the API gateway level for centralized enforcement. Use local counters with periodic sync for approximate but faster limiting.\\n\\n\\n- Centralized counters are accurate but add network latency; local counters are fast but may allow brief bursts above limits\\n- Token bucket allows bursts up to bucket size; sliding window log is precise but uses more memory`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement a Sliding Window Rate Limiter: Build a rate limiter that uses the sliding window log algorithm. Track timestamps of each request in a sorted set. For each incoming request, remove entries ou", "Build a Token Bucket Implementation: Create a token bucket rate limiter where tokens refill at a fixed rate. Each request consumes one token. If the bucket is empty, reject the request. Support config", "Problem: Rate limits are not enforced accurately in a multi-node deployment, allowing users to exceed limits by hitting different servers. Solution: Use a centralized Redis instance for counter storag", "Problem: Redis becomes a single point of failure, and its downtime disables all rate limiting. Solution: Implement a local in-memory fallback rate limiter that activates when Redis is unavailable. Use"],
  },
  {
    id: 9003,
    description: `- Support push notifications, SMS, email, and in-app channels\\n- Handle millions of notifications per day with at-least-once delivery\\n- Support user preferences, templates, scheduling, and priority levels\\n- Provide delivery tracking and analytics\\n`,
    examples: `Exercise 1: Design a Priority Queue System\\nImplement a notification prioritization system with three levels: critical (OTP/2FA), high (order updates), and normal (marketing). Ensure critical notifications are processed within seconds even when millions of normal notifications are queued.\\n\\nExercise 2: Build a Rate-Limited Provider Adapter\\nCreate an adapter for an SMS provider that enforces rate limits (e.g., 100 messages/second). Buffer excess messages and retry with exponential backoff when the provider returns 429 or 503 errors.`,
    intuition: `A notification service is an event-driven pipeline that transforms a notification intent into multi-channel deliveries while respecting user preferences. The key challenge is reliable delivery at scale without overwhelming downstream providers or annoying users with duplicates.`,
    approach: `A notification API accepts requests and publishes to a message queue (Kafka). Channel-specific workers consume messages and dispatch via third-party providers (APNs, FCM, Twilio, SendGrid). A preference service filters and routes notifications. A tracking service records delivery status.\\n\\n\\n- **API Gateway**: Accepts notification requests, validates, and enqueues\\n- **Message Queue**: Kafka topics partitioned by channel type for parallel processing\\n- **Channel Workers**: Dedicated consumers for each channel with provider-specific adapters\\n- **Preference Service**: Stores user opt-in/out, quiet hours, and channel preferences\\n- **Template Engine**: Renders notification content from templates with variable substitution\\n`,
    code: `# System Design: Design a Notification Service\\n#\\n# Key Components:\\n# - API Gateway: Accepts notification requests, validates, and enqueues\\n# - Message Queue: Kafka topics partitioned by channel type for parallel processing\\n# - Channel Workers: Dedicated consumers for each channel with provider-specific adapters\\n# - Preference Service: Stores user opt-in/out, quiet hours, and channel preferences\\n# - Template Engine: Renders notification content from templates with variable substitution\\n#\\n# Architecture Notes:\\n# A notification API accepts requests and publishes to a message queue (Kafka). Channel-specific worke\\n`,
    explanation: `Scale workers independently per channel based on volume. Use Kafka partitions for parallelism. Batch SMS and email sends for efficiency. Use priority queues to ensure critical notifications (2FA codes) are not delayed by marketing blasts.\\n\\n\\n- At-least-once delivery may cause duplicates; exactly-once adds complexity and latency\\n- Third-party provider dependencies require fallback providers and circuit breakers`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Design a Priority Queue System: Implement a notification prioritization system with three levels: critical (OTP/2FA), high (order updates), and normal (marketing). Ensure critical notifications are pr", "Build a Rate-Limited Provider Adapter: Create an adapter for an SMS provider that enforces rate limits (e.g., 100 messages/second). Buffer excess messages and retry with exponential backoff when the p", "Problem: Users receive duplicate notifications for the same event, especially during retries. Solution: Implement idempotency by generating a unique notification ID per event and checking a deduplicat", "Problem: Notification delivery latency spikes during marketing campaign blasts, delaying critical OTP notifications. Solution: Separate queues by priority with dedicated worker pools. Reserve capacity"],
  },
  {
    id: 9004,
    description: `- Support 1:1 and group messaging with real-time delivery\\n- Handle online/offline status, read receipts, and typing indicators\\n- Store message history and sync across devices\\n- Support media sharing (images, videos, documents)\\n- Scale to billions of messages per day\\n`,
    examples: `Exercise 1: Implement a Message Sync Protocol\\nDesign a protocol where clients track the last message sequence number they received. On reconnection, the client sends its last sequence number and the server sends all missed messages. Handle the case where history is too large by paginating.\\n\\nExercise 2: Design Group Message Fanout\\nBuild a system that delivers a message to all members of a group chat. For small groups (< 100), fan out on write. For large groups (100+), fan out on read. Implement both strategies and a routing decision layer.`,
    intuition: `A chat system is a real-time message routing network where persistent connections enable instant delivery and a store-and-forward mechanism handles offline users. The core challenge is maintaining millions of stateful WebSocket connections while ensuring no messages are lost.`,
    approach: `Clients maintain persistent WebSocket connections to chat servers. A connection manager tracks which user is connected to which server. Messages are routed through a message service that stores them in a database and forwards to recipients. Offline messages are queued for later delivery. Media is uploaded to object storage with CDN.\\n\\n\\n- **WebSocket Servers**: Maintain persistent bidirectional connections with clients\\n- **Connection Manager**: Maps user IDs to WebSocket server addresses (Redis)\\n- **Message Service**: Persists messages and routes them to recipient connections\\n- **Group Service**: Manages group membership and fans out messages to members\\n- **Media Service**: Handles upload, compression, thumbnail generation, and CDN distribution\\n`,
    code: `# System Design: Design a Chat System (WhatsApp)\\n#\\n# Key Components:\\n# - WebSocket Servers: Maintain persistent bidirectional connections with clients\\n# - Connection Manager: Maps user IDs to WebSocket server addresses (Redis)\\n# - Message Service: Persists messages and routes them to recipient connections\\n# - Group Service: Manages group membership and fans out messages to members\\n# - Media Service: Handles upload, compression, thumbnail generation, and CDN distribution\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition WebSocket servers by user ID ranges. Use Kafka for async message fanout in large groups. Shard message storage by conversation ID. Use a push notification fallback when WebSocket is disconnected.\\n\\n\\n- WebSockets provide real-time delivery but require stateful connections and sticky routing\\n- Fan-out on write (pre-deliver to all members) is fast for reads but expensive for large groups`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement a Message Sync Protocol: Design a protocol where clients track the last message sequence number they received. On reconnection, the client sends its last sequence number and the server sends", "Design Group Message Fanout: Build a system that delivers a message to all members of a group chat. For small groups (< 100), fan out on write. For large groups (100+), fan out on read. Implement both", "Problem: Messages are delivered out of order or lost when the WebSocket server crashes mid-delivery. Solution: Persist messages to the database before acknowledging to the sender. Use message sequence", "Problem: Group chats with thousands of members cause massive write amplification and high latency. Solution: Switch to fan-out on read for groups above a threshold. Store one copy of the message and h"],
  },
  {
    id: 9005,
    description: `- Users post content and see a personalized feed of posts from followed accounts\\n- Feed generation must handle celebrity accounts with millions of followers\\n- Support ranking by relevance, not just chronological order\\n- Handle text, images, and video content with real-time updates\\n`,
    examples: `Exercise 1: Implement Hybrid Fanout Logic\\nBuild a fanout service that decides between push and pull based on follower count. Users with < 10,000 followers get fan-out on write. Celebrity posts are merged at read time. Implement the feed assembly that combines both sources.\\n\\nExercise 2: Design a Feed Ranking System\\nCreate a ranking function that scores posts based on: recency (time decay), engagement (likes, comments, shares), relationship strength (interaction frequency with the author), and content type preference. Return the top N posts.`,
    intuition: `A news feed system is a content distribution network that balances pre-computation (push) for fast reads against on-demand assembly (pull) for high-follower accounts. The hybrid fanout model is the key insight that makes platforms like Twitter and Instagram scale.`,
    approach: `A post service stores new content and triggers fanout. A feed service assembles personalized timelines. For regular users, fan-out on write pre-computes feeds in a cache (Redis sorted sets). For celebrity accounts, fan-out on read merges their posts at query time. A ranking service scores and orders posts.\\n\\n\\n- **Post Service**: Stores posts in a database and publishes events to Kafka\\n- **Fanout Service**: Pushes post IDs to follower feed caches for normal users\\n- **Feed Cache**: Redis sorted sets per user storing ranked post IDs\\n- **Feed Service**: Assembles feed by merging cached posts with celebrity posts at read time\\n- **Ranking Service**: ML-based scoring considering engagement, recency, and user preferences\\n`,
    code: `# System Design: Design a News Feed (Twitter/Instagram)\\n#\\n# Key Components:\\n# - Post Service: Stores posts in a database and publishes events to Kafka\\n# - Fanout Service: Pushes post IDs to follower feed caches for normal users\\n# - Feed Cache: Redis sorted sets per user storing ranked post IDs\\n# - Feed Service: Assembles feed by merging cached posts with celebrity posts at read time\\n# - Ranking Service: ML-based scoring considering engagement, recency, and user preferences\\n#\\n# Architecture Notes:\\n# A post service stores new content and triggers fanout. A feed service assembles personalized timelin\\n`,
    explanation: `Use a hybrid fanout approach: push for users with fewer followers, pull for celebrities. Shard feed caches by user ID. Use a graph database or adjacency list for the follow relationship. Pre-compute and cache trending content.\\n\\n\\n- Fan-out on write is fast for reads but slow and expensive for celebrity posts with millions of followers\\n- Chronological feeds are simple but less engaging; ranked feeds increase engagement but add ML complexity`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Hybrid Fanout Logic: Build a fanout service that decides between push and pull based on follower count. Users with < 10,000 followers get fan-out on write. Celebrity posts are merged at read", "Design a Feed Ranking System: Create a ranking function that scores posts based on: recency (time decay), engagement (likes, comments, shares), relationship strength (interaction frequency with the au", "Problem: A celebrity with 50 million followers publishes a post, and the fanout service takes minutes to update all follower feeds. Solution: Implement hybrid fanout: skip cache pre-computation for us", "Problem: Feed cache memory usage grows unbounded as users accumulate thousands of posts in their timelines. Solution: Limit feed cache to the most recent N posts (e.g., 800). Evict caches for users in"],
  },
  {
    id: 9006,
    description: `- Upload, download, and sync files across multiple devices\\n- Support large files (up to several GB) with resumable uploads\\n- Detect and sync only changed portions of files (delta sync)\\n- Handle concurrent edits with conflict resolution\\n- Provide file versioning and sharing capabilities\\n`,
    examples: `Exercise 1: Implement Content-Defined Chunking\\nBuild a chunking algorithm using a rolling hash (e.g., Rabin fingerprint) that splits a file into variable-size chunks at content-defined boundaries. Chunks should average 4MB with min 1MB and max 8MB.\\n\\nExercise 2: Design a Conflict Resolution System\\nImplement a system that detects when two clients edit the same file simultaneously. Use vector clocks or version numbers to detect conflicts. When a conflict occurs, save both versions and let the user resolve.`,
    intuition: `A file storage system is a chunk-based synchronization engine that breaks files into blocks, deduplicates them, and only transfers changed chunks across devices. The delta sync mechanism is what makes it bandwidth-efficient and fast.`,
    approach: `Clients use a sync agent that monitors local file changes and communicates with a metadata service. Files are split into chunks (4MB blocks), deduplicated, and stored in object storage (S3). A metadata database tracks file hierarchy, chunks, and versions. A notification service pushes sync events to connected clients.\\n\\n\\n- **Sync Agent**: Client-side daemon that watches file changes and computes deltas\\n- **Chunking Service**: Splits files into fixed or content-defined chunks using rolling hash (Rabin fingerprinting)\\n- **Metadata Service**: Tracks file paths, chunk lists, versions, and permissions in a relational DB\\n- **Block Storage**: S3-compatible object store holding deduplicated chunks\\n- **Notification Service**: Long polling or WebSocket to alert clients of remote changes\\n`,
    code: `# System Design: Design a File Storage System (Dropbox)\\n#\\n# Key Components:\\n# - Sync Agent: Client-side daemon that watches file changes and computes deltas\\n# - Chunking Service: Splits files into fixed or content-defined chunks using rolling hash (Rabin fingerprinting)\\n# - Metadata Service: Tracks file paths, chunk lists, versions, and permissions in a relational DB\\n# - Block Storage: S3-compatible object store holding deduplicated chunks\\n# - Notification Service: Long polling or WebSocket to alert clients of remote changes\\n#\\n# Architecture Notes:\\n`,
    explanation: `Chunk deduplication reduces storage by 30-50%. Use content-defined chunking so insertions don't invalidate all chunks. Shard metadata by user ID. Use CDN for frequently downloaded files.\\n\\n\\n- Fixed-size chunks are simpler but wasteful on edits; content-defined chunks optimize delta sync but add CPU cost\\n- Strong consistency for metadata ensures correctness but adds latency; eventual consistency risks conflicts`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Content-Defined Chunking: Build a chunking algorithm using a rolling hash (e.g., Rabin fingerprint) that splits a file into variable-size chunks at content-defined boundaries. Chunks should ", "Design a Conflict Resolution System: Implement a system that detects when two clients edit the same file simultaneously. Use vector clocks or version numbers to detect conflicts. When a conflict occur", "Problem: Uploading a large 5GB file fails at 80% completion, and the user must restart from the beginning. Solution: Implement chunked, resumable uploads. Track which chunks have been received server-", "Problem: A small edit to a large file causes the entire file to be re-uploaded and re-synced to all devices. Solution: Use content-defined chunking with a rolling hash so chunk boundaries depend on co"],
  },
  {
    id: 9007,
    description: `- Upload, transcode, and stream videos to millions of concurrent viewers\\n- Support adaptive bitrate streaming for varying network conditions\\n- Handle content recommendation, search, and engagement features\\n- Minimize startup latency and buffering during playback\\n`,
    examples: `Exercise 1: Design a Transcoding Pipeline\\nBuild a DAG-based transcoding workflow where an uploaded video is split into segments, each segment is transcoded to multiple resolutions in parallel, and results are merged into a streaming manifest. Handle failures by retrying individual segment-resolution pairs.\\n\\nExercise 2: Implement Adaptive Bitrate Selection\\nCreate a client-side algorithm that monitors download throughput and buffer level, then selects the optimal video quality for the next segment. Switch up gradually but drop quality immediately when bandwidth drops.`,
    intuition: `A video streaming platform is a content processing and distribution pipeline that transforms raw uploads into multi-bitrate segments served from edge locations close to viewers. Adaptive bitrate streaming is the key technique that enables smooth playback across varying network conditions.`,
    approach: `Uploaded videos go through a transcoding pipeline that produces multiple resolutions and formats. Transcoded segments are stored on object storage and distributed via CDN. A video metadata service stores titles, descriptions, and engagement data. An adaptive streaming protocol (HLS/DASH) serves video segments based on client bandwidth.\\n\\n\\n- **Upload Service**: Accepts video uploads, validates, and queues for transcoding\\n- **Transcoding Pipeline**: Distributed workers (FFmpeg-based) that encode videos into multiple bitrates (240p to 4K) and segment them\\n- **CDN**: Edge servers cache popular video segments close to viewers\\n- **Streaming Service**: Serves manifest files (m3u8/mpd) and video segments via HTTP\\n- **Recommendation Engine**: ML-based collaborative filtering and content-based suggestions\\n`,
    code: `# System Design: Design a Video Streaming Platform (YouTube)\\n#\\n# Key Components:\\n# - Upload Service: Accepts video uploads, validates, and queues for transcoding\\n# - Transcoding Pipeline: Distributed workers (FFmpeg-based) that encode videos into multiple bitrates (240p to 4K) and segment them\\n# - CDN: Edge servers cache popular video segments close to viewers\\n# - Streaming Service: Serves manifest files (m3u8/mpd) and video segments via HTTP\\n# - Recommendation Engine: ML-based collaborative filtering and content-based suggestions\\n#\\n# Architecture Notes:\\n`,
    explanation: `Use a DAG-based transcoding pipeline for parallel processing of different resolutions. Pre-warm CDN for trending videos. Use edge computing for live stream processing. Shard metadata by video ID.\\n\\n\\n- More transcoding profiles cover more devices but multiply storage and processing costs\\n- Pre-loading video segments reduces buffering but wastes bandwidth if the user navigates away`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Design a Transcoding Pipeline: Build a DAG-based transcoding workflow where an uploaded video is split into segments, each segment is transcoded to multiple resolutions in parallel, and results are me", "Implement Adaptive Bitrate Selection: Create a client-side algorithm that monitors download throughput and buffer level, then selects the optimal video quality for the next segment. Switch up graduall", "Problem: A viral video causes CDN cache misses and overwhelms the origin storage with millions of concurrent requests. Solution: Implement request coalescing at the CDN (only one origin fetch per cach", "Problem: The transcoding pipeline creates a huge backlog during peak upload hours, delaying video availability by hours. Solution: Transcode the most common resolution (720p) first and make the video "],
  },
  {
    id: 9008,
    description: `- Support GET, PUT, DELETE operations with single-digit millisecond latency\\n- Handle billions of key-value pairs across a distributed cluster\\n- Provide tunable consistency (strong or eventual) and high availability\\n- Support automatic partitioning, replication, and failure recovery\\n`,
    examples: `Exercise 1: Implement Consistent Hashing with Virtual Nodes\\nBuild a consistent hashing ring that maps keys to nodes. Support adding and removing nodes with minimal key reassignment. Use 150 virtual nodes per physical node to ensure even distribution.\\n\\nExercise 2: Build a Quorum Read/Write System\\nImplement a coordinator that sends write requests to N replicas and waits for W acknowledgments. For reads, query N replicas, wait for R responses, and return the value with the highest version number. Make W and R configurable.`,
    intuition: `A distributed key-value store is a partitioned, replicated hash map that uses consistent hashing for data distribution and quorum protocols for tunable consistency. The CAP theorem trade-off between consistency and availability is controlled by adjusting quorum parameters.`,
    approach: `Data is partitioned across nodes using consistent hashing with virtual nodes. Each partition is replicated to N nodes for durability. A coordinator node handles client requests and manages quorum reads/writes. An anti-entropy protocol (Merkle trees) detects and repairs inconsistencies between replicas.\\n\\n\\n- **Consistent Hash Ring**: Maps keys to nodes with virtual nodes for balanced distribution\\n- **Replication Manager**: Copies data to N successor nodes on the hash ring\\n- **Quorum Protocol**: Configurable W (write) and R (read) quorum values (W + R > N for strong consistency)\\n- **Gossip Protocol**: Nodes exchange heartbeats to detect failures and propagate membership changes\\n- **Merkle Trees**: Per-partition hash trees for efficient replica synchronization\\n`,
    code: `# System Design: Design a Key-Value Store (Redis/DynamoDB)\\n#\\n# Key Components:\\n# - Consistent Hash Ring: Maps keys to nodes with virtual nodes for balanced distribution\\n# - Replication Manager: Copies data to N successor nodes on the hash ring\\n# - Quorum Protocol: Configurable W (write) and R (read) quorum values (W + R > N for strong consistency)\\n# - Gossip Protocol: Nodes exchange heartbeats to detect failures and propagate membership changes\\n# - Merkle Trees: Per-partition hash trees for efficient replica synchronization\\n#\\n# Architecture Notes:\\n`,
    explanation: `Add nodes and rebalance partitions with minimal data movement. Use virtual nodes to handle heterogeneous hardware. Implement hinted handoff for temporary failure handling. Use SSTables and LSM trees for efficient disk storage.\\n\\n\\n- W=1 gives fast writes but risks data loss; W=N ensures durability but is slow and less available\\n- LSM trees optimize writes but reads may check multiple levels; B-trees optimize reads but have write amplification`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Consistent Hashing with Virtual Nodes: Build a consistent hashing ring that maps keys to nodes. Support adding and removing nodes with minimal key reassignment. Use 150 virtual nodes per phy", "Build a Quorum Read/Write System: Implement a coordinator that sends write requests to N replicas and waits for W acknowledgments. For reads, query N replicas, wait for R responses, and return the val", "Problem: After adding new nodes to the cluster, some keys become temporarily unavailable or return stale data. Solution: Implement a two-phase rebalancing: first copy data to new owners while old owne", "Problem: Concurrent writes to the same key on different replicas create conflicting versions that cannot be automatically resolved. Solution: Use vector clocks to track causal relationships between ve"],
  },
  {
    id: 9009,
    description: `- Crawl billions of web pages and build a searchable index\\n- Return relevant results for keyword queries in under 200ms\\n- Rank results by relevance using link analysis and content signals\\n- Handle typos, synonyms, and natural language queries\\n- Update the index continuously as web content changes\\n`,
    examples: `Exercise 1: Build an Inverted Index\\nCreate a system that processes a collection of documents, tokenizes them, removes stop words, applies stemming, and builds an inverted index. Support boolean AND/OR queries and phrase matching using positional indexes.\\n\\nExercise 2: Implement a Simple PageRank\\nBuild a simplified PageRank algorithm over a graph of web pages. Iteratively compute authority scores where each page distributes its rank equally among outgoing links. Include a damping factor of 0.85. Run until convergence.`,
    intuition: `A search engine is a pipeline that transforms the web into an inverted index and uses ranking algorithms to surface the most relevant documents for any query. The inverted index is the fundamental data structure that makes sub-second search over billions of documents possible.`,
    approach: `A web crawler discovers and fetches pages. A document processor extracts text, normalizes content, and builds an inverted index. The index is sharded across thousands of machines. A query engine parses queries, searches relevant shards, merges results, and ranks them. A PageRank-like algorithm provides authority scores.\\n\\n\\n- **Web Crawler**: Distributed crawler with URL frontier, politeness policies, and deduplication\\n- **Inverted Index**: Maps terms to sorted lists of (document_id, term_frequency, positions)\\n- **Query Parser**: Handles boolean operators, phrases, spelling correction, and query expansion\\n- **Ranking Engine**: Combines TF-IDF, PageRank, freshness, and user engagement signals\\n- **Index Sharding**: Partition index by document ID (document-partitioned) or by term (term-partitioned)\\n`,
    code: `# System Design: Design a Search Engine (Google)\\n#\\n# Key Components:\\n# - Web Crawler: Distributed crawler with URL frontier, politeness policies, and deduplication\\n# - Inverted Index: Maps terms to sorted lists of (document_id, term_frequency, positions)\\n# - Query Parser: Handles boolean operators, phrases, spelling correction, and query expansion\\n# - Ranking Engine: Combines TF-IDF, PageRank, freshness, and user engagement signals\\n# - Index Sharding: Partition index by document ID (document-partitioned) or by term (term-partitioned)\\n#\\n# Architecture Notes:\\n# A web crawler discovers and fetches pages. A document processor extracts text, normalizes content, a\\n`,
    explanation: `Document-partitioned indexing allows each shard to independently score documents. Fan-out queries to all shards and merge top-K results. Use tiered indexing: a fast tier for popular pages and a full tier for comprehensive results. Cache frequent queries.\\n\\n\\n- Document-partitioned sharding enables independent scoring but requires querying all shards; term-partitioned reduces fan-out but complicates ranking\\n- Fresher index requires more crawling resources; stale index saves resources but returns outdated results`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build an Inverted Index: Create a system that processes a collection of documents, tokenizes them, removes stop words, applies stemming, and builds an inverted index. Support boolean AND/OR queries an", "Implement a Simple PageRank: Build a simplified PageRank algorithm over a graph of web pages. Iteratively compute authority scores where each page distributes its rank equally among outgoing links. In", "Problem: Query latency increases as the index grows, with tail latencies exceeding 2 seconds for rare term queries. Solution: Use tiered indexing with a small fast tier containing popular documents qu", "Problem: The web crawler gets trapped in infinite loops or spider traps, wasting resources on low-value pages. Solution: Implement URL normalization to canonicalize URLs. Set a maximum depth per domai"],
  },
  {
    id: 9010,
    description: `- Provide sub-millisecond GET/SET operations for cached data\\n- Distribute cached data across multiple nodes with consistent hashing\\n- Handle node failures gracefully without losing all cached data\\n- Support TTL-based expiration and LRU eviction policies\\n- Scale horizontally by adding more cache nodes\\n`,
    examples: `Exercise 1: Implement an LRU Cache with O(1) Operations\\nBuild an LRU cache using a hash map and a doubly-linked list. GET moves the item to the head. SET adds to the head and evicts the tail if at capacity. Both operations must be O(1) time complexity.\\n\\nExercise 2: Design a Cache Warming Strategy\\nWhen a new cache node is added to the cluster, design a system that pre-populates it with likely-to-be-requested data to avoid a thundering herd of cache misses hitting the database. Consider using access logs and request sampling.`,
    intuition: `A distributed cache is a partitioned in-memory hash table that trades durability for speed, serving as a buffer between application servers and databases. The key design challenge is maintaining high hit rates while handling node failures without cascading load to the database.`,
    approach: `Clients use a consistent hashing library to route requests directly to the appropriate cache node (no central coordinator). Each cache node stores key-value pairs in a hash table with a doubly-linked list for LRU eviction. A slab allocator manages memory to avoid fragmentation. Monitoring tracks hit rates and eviction counts.\\n\\n\\n- **Client Library**: Implements consistent hashing, connection pooling, and failover logic\\n- **Hash Table**: O(1) lookups using chained hashing within each cache node\\n- **LRU Eviction**: Doubly-linked list tracks access order; evicts least recently used items when memory is full\\n- **Slab Allocator**: Pre-allocates memory in size classes to reduce fragmentation\\n- **TTL Manager**: Lazy expiration on access plus periodic background sweep of expired keys\\n`,
    code: `# System Design: Design a Distributed Cache (Memcached)\\n#\\n# Key Components:\\n# - Client Library: Implements consistent hashing, connection pooling, and failover logic\\n# - Hash Table: O(1) lookups using chained hashing within each cache node\\n# - LRU Eviction: Doubly-linked list tracks access order; evicts least recently used items when memory is full\\n# - Slab Allocator: Pre-allocates memory in size classes to reduce fragmentation\\n# - TTL Manager: Lazy expiration on access plus periodic background sweep of expired keys\\n#\\n# Architecture Notes:\\n`,
    explanation: `Add nodes to the hash ring to increase capacity. Use virtual nodes for balanced distribution. Implement warm-up strategies when adding nodes to avoid cold-start cache misses. Use mcrouter or similar proxy for advanced routing.\\n\\n\\n- No replication means node failure loses that shard's cache (fast rebuild from DB vs. replicated cache cost)\\n- Larger items reduce effective cache capacity; compress values to fit more but add CPU overhead`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement an LRU Cache with O(1) Operations: Build an LRU cache using a hash map and a doubly-linked list. GET moves the item to the head. SET adds to the head and evicts the tail if at capacity. Both", "Design a Cache Warming Strategy: When a new cache node is added to the cluster, design a system that pre-populates it with likely-to-be-requested data to avoid a thundering herd of cache misses hittin", "Problem: A cache node crashes and the sudden flood of cache misses overwhelms the database, causing a cascading failure. Solution: Use consistent hashing so only keys from the failed node redistribute", "Problem: A popular item expires from cache and thousands of concurrent requests simultaneously try to rebuild it from the database. Solution: Implement cache stampede protection using a mutex: the fir"],
  },
  {
    id: 9011,
    description: `- Support product catalog with search, browsing, and filtering\\n- Handle shopping cart, checkout, and order management\\n- Process millions of concurrent users during flash sales\\n- Manage inventory in real-time to prevent overselling\\n- Support personalized recommendations and reviews\\n`,
    examples: `Exercise 1: Implement a Checkout Saga\\nDesign a saga orchestrator for the checkout process: reserve inventory -> process payment -> create order -> schedule shipping. Implement compensating transactions for each step (e.g., release inventory if payment fails).\\n\\nExercise 2: Design an Inventory System for Flash Sales\\nBuild an inventory management system that handles 100,000 concurrent purchase attempts for a limited-stock item (e.g., 100 units). Ensure no overselling and fair ordering. Minimize contention on the stock counter.`,
    intuition: `An e-commerce platform is a choreography of microservices where the checkout flow is the most critical path, requiring saga-based distributed transactions to coordinate inventory, payment, and order creation. Inventory management under concurrent access is the hardest correctness problem.`,
    approach: `A microservices architecture with dedicated services for catalog, cart, order, payment, inventory, and recommendation. An API gateway routes requests and handles authentication. An event bus (Kafka) enables async communication between services. Each service owns its data store, following the database-per-service pattern.\\n\\n\\n- **Catalog Service**: Product data in Elasticsearch for full-text search, with a relational DB as source of truth\\n- **Cart Service**: Redis-backed session store for active carts with persistence to database\\n- **Order Service**: Orchestrates the checkout workflow using a saga pattern across inventory, payment, and shipping\\n- **Inventory Service**: Manages stock counts with optimistic locking to prevent overselling\\n- **Recommendation Engine**: Collaborative filtering using user purchase and browsing history\\n`,
    code: `# System Design: Design an E-Commerce Platform (Amazon)\\n#\\n# Key Components:\\n# - Catalog Service: Product data in Elasticsearch for full-text search, with a relational DB as source of truth\\n# - Cart Service: Redis-backed session store for active carts with persistence to database\\n# - Order Service: Orchestrates the checkout workflow using a saga pattern across inventory, payment, and shipping\\n# - Inventory Service: Manages stock counts with optimistic locking to prevent overselling\\n# - Recommendation Engine: Collaborative filtering using user purchase and browsing history\\n#\\n# Architecture Notes:\\n# A microservices architecture with dedicated services for catalog, cart, order, payment, inventory, a\\n`,
    explanation: `Use CQRS to separate read and write models for the catalog. Cache product pages at the CDN level. Use event-driven inventory updates. Pre-compute and cache recommendation results. Auto-scale services independently based on load.\\n\\n\\n- Microservices enable independent scaling but introduce distributed transaction complexity (saga vs. 2PC)\\n- Optimistic locking for inventory is performant but may reject valid orders under extreme contention`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement a Checkout Saga: Design a saga orchestrator for the checkout process: reserve inventory -> process payment -> create order -> schedule shipping. Implement compensating transactions for each ", "Design an Inventory System for Flash Sales: Build an inventory management system that handles 100,000 concurrent purchase attempts for a limited-stock item (e.g., 100 units). Ensure no overselling and", "Problem: During a flash sale, the inventory database becomes a bottleneck and orders are rejected or duplicated due to lock contention. Solution: Move hot inventory counts to Redis with atomic DECR op", "Problem: A payment service failure leaves orders in an inconsistent state: inventory reserved but payment not processed, causing phantom stock reductions. Solution: Implement a saga with compensating "],
  },
  {
    id: 9012,
    description: `- Process credit card, debit card, and bank transfer payments\\n- Ensure exactly-once payment processing with idempotency\\n- Support refunds, chargebacks, and multi-currency transactions\\n- Comply with PCI-DSS security standards\\n- Achieve 99.999% availability for payment processing\\n`,
    examples: `Exercise 1: Implement Idempotent Payment Processing\\nBuild a payment endpoint that accepts an idempotency key with each request. If the same key is seen again, return the original response without reprocessing. Handle edge cases: concurrent requests with the same key, and requests that failed midway.\\n\\nExercise 2: Design a Double-Entry Ledger\\nCreate a ledger system where every payment creates two entries: a debit from the payer's account and a credit to the payee's account. Ensure the sum of all debits always equals the sum of all credits. Support querying account balances.`,
    intuition: `A payment system is a ledger-based transaction processor where idempotency and exactly-once semantics are non-negotiable requirements because duplicate charges directly cost money and erode trust. The double-entry bookkeeping model ensures every financial movement is traceable and balanced.`,
    approach: `A payment gateway receives payment requests, tokenizes card data, and routes to a payment processor. A ledger service maintains double-entry bookkeeping for all transactions. An idempotency layer prevents duplicate charges. A reconciliation service matches internal records with bank statements. All card data flows through PCI-compliant isolated infrastructure.\\n\\n\\n- **Payment Gateway**: Entry point that validates, rate-limits, and routes payment requests\\n- **Tokenization Service**: Replaces card numbers with tokens in a PCI-compliant vault\\n- **Ledger Service**: Double-entry accounting (every debit has a corresponding credit)\\n- **Idempotency Layer**: Stores idempotency keys with request hashes to detect and deduplicate retries\\n- **Reconciliation Engine**: Batch process that compares internal ledger with external bank/PSP records\\n`,
    code: `# System Design: Design a Payment System (Stripe)\\n#\\n# Key Components:\\n# - Payment Gateway: Entry point that validates, rate-limits, and routes payment requests\\n# - Tokenization Service: Replaces card numbers with tokens in a PCI-compliant vault\\n# - Ledger Service: Double-entry accounting (every debit has a corresponding credit)\\n# - Idempotency Layer: Stores idempotency keys with request hashes to detect and deduplicate retries\\n# - Reconciliation Engine: Batch process that compares internal ledger with external bank/PSP records\\n#\\n# Architecture Notes:\\n# A payment gateway receives payment requests, tokenizes card data, and routes to a payment processor.\\n`,
    explanation: `Use async processing for non-critical paths (notifications, analytics). Synchronous processing only for the critical payment path. Shard the ledger by merchant ID. Use active-active deployment across regions for availability.\\n\\n\\n- Synchronous payment processing ensures immediate confirmation but adds latency; async is faster but requires webhooks\\n- Double-entry ledger provides audit trail but doubles write volume compared to single-entry`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Idempotent Payment Processing: Build a payment endpoint that accepts an idempotency key with each request. If the same key is seen again, return the original response without reprocessing. H", "Design a Double-Entry Ledger: Create a ledger system where every payment creates two entries: a debit from the payer's account and a credit to the payee's account. Ensure the sum of all debits always ", "Problem: A customer is charged twice for the same purchase because the client retried after a network timeout, but the first request had already succeeded. Solution: Require an idempotency key with ev", "Problem: End-of-day reconciliation reveals a mismatch between the internal ledger and the bank's settlement report. Solution: Run automated reconciliation that flags discrepancies. For internal-only r"],
  },
  {
    id: 9013,
    description: `- Search available rooms by location, dates, guests, and price range\\n- Handle concurrent booking attempts without double-booking\\n- Support cancellations, modifications, and dynamic pricing\\n- Display real-time availability across thousands of hotels\\n- Handle peak loads during holiday seasons\\n`,
    examples: `Exercise 1: Implement Room Availability Checking\\nDesign a database schema and query that efficiently checks if a specific room type is available for a given date range. Handle partial overlaps correctly. The system should support checking availability across hundreds of rooms for a 1-7 night stay.\\n\\nExercise 2: Design a Concurrent Booking Handler\\nBuild a booking endpoint that handles multiple users trying to book the last available room simultaneously. Ensure exactly one booking succeeds and others receive a sold-out response. Use database-level locking.`,
    intuition: `A hotel booking system is an inventory management problem where the core challenge is preventing double-bookings under concurrent access while maintaining responsive search across millions of room-night combinations. Date-range availability queries are the distinguishing complexity.`,
    approach: `A search service backed by Elasticsearch handles availability queries. A booking service manages reservations with pessimistic locking on room inventory. A pricing service computes dynamic rates based on demand, season, and competition. An event-driven architecture publishes booking events for analytics, notifications, and partner systems.\\n\\n\\n- **Search Service**: Elasticsearch with filters for location (geo queries), dates, price, amenities, and ratings\\n- **Availability Service**: Manages room inventory using a date-range availability table with row-level locking\\n- **Booking Service**: Handles reservation creation, confirmation, modification, and cancellation\\n- **Pricing Engine**: Dynamic pricing based on occupancy rate, day of week, season, and competitor rates\\n- **Hotel Management Portal**: Interface for hotels to update room details, pricing rules, and blackout dates\\n`,
    code: `# System Design: Design a Hotel Booking System (Booking.com)\\n#\\n# Key Components:\\n# - Search Service: Elasticsearch with filters for location (geo queries), dates, price, amenities, and ratings\\n# - Availability Service: Manages room inventory using a date-range availability table with row-level locking\\n# - Booking Service: Handles reservation creation, confirmation, modification, and cancellation\\n# - Pricing Engine: Dynamic pricing based on occupancy rate, day of week, season, and competitor rates\\n# - Hotel Management Portal: Interface for hotels to update room details, pricing rules, and blackout dates\\n#\\n# Architecture Notes:\\n# A search service backed by Elasticsearch handles availability queries. A booking service manages res\\n`,
    explanation: `Cache search results with short TTLs for popular destinations. Use read replicas for search queries. Shard availability data by hotel ID. Pre-compute availability for popular date ranges. Use a CDN for hotel images and static content.\\n\\n\\n- Pessimistic locking prevents double-booking but reduces concurrency; optimistic locking improves concurrency but increases retry rates\\n- Showing cached availability is fast but may lead to failed bookings; real-time checks are accurate but slow`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Room Availability Checking: Design a database schema and query that efficiently checks if a specific room type is available for a given date range. Handle partial overlaps correctly. The sys", "Design a Concurrent Booking Handler: Build a booking endpoint that handles multiple users trying to book the last available room simultaneously. Ensure exactly one booking succeeds and others receive ", "Problem: Two users simultaneously book the last available room for the same dates, resulting in a double-booking. Solution: Use pessimistic locking (SELECT FOR UPDATE) on the availability rows for the", "Problem: Search results show rooms as available, but when users try to book, they get 'sold out' errors, creating a frustrating experience. Solution: Use short cache TTLs (30-60 seconds) for high-dema"],
  },
  {
    id: 9014,
    description: `- Sell tickets for events with specific seat assignments\\n- Handle extreme traffic spikes when popular events go on sale (millions of users in seconds)\\n- Prevent overselling and ensure fair access\\n- Support seat selection, holds, and time-limited checkout\\n- Manage a virtual waiting queue for high-demand events\\n`,
    examples: `Exercise 1: Design a Virtual Waiting Queue\\nBuild a queue system that assigns each user a position when they arrive for a popular event. Admit users in batches (e.g., 1000 at a time) based on system capacity. Show estimated wait times. Handle users who leave the queue.\\n\\nExercise 2: Implement Seat Hold with TTL\\nCreate a system where selecting a seat places a temporary hold (5 minutes). If the user does not complete checkout, the hold expires and the seat becomes available again. Handle the race condition of hold expiration during payment processing.`,
    intuition: `A ticket booking system is an inventory reservation problem under extreme concurrency where a virtual queue is essential to manage demand that can exceed capacity by 100x in the first seconds of an on-sale event. Seat-level locking with time-bounded holds is the mechanism that prevents both overselling and indefinite resource blocking.`,
    approach: `A virtual queue (waiting room) absorbs traffic spikes and meters users into the booking flow. A seat map service shows real-time availability. A booking service uses optimistic locking with seat-level granularity. A timer service releases held seats after a checkout timeout. A separate inventory service pre-loads seat data into Redis for fast lookups.\\n\\n\\n- **Virtual Queue**: Assigns queue positions and gradually admits users based on available capacity\\n- **Seat Map Service**: Real-time seat availability backed by Redis bitmaps per event section\\n- **Hold Service**: Temporarily locks selected seats for a user with a 5-10 minute expiration\\n- **Booking Service**: Confirms purchase, charges payment, and issues e-tickets\\n- **Inventory Service**: Redis-backed seat status (available/held/sold) with atomic transitions\\n`,
    code: `# System Design: Design a Ticket Booking System (Ticketmaster)\\n#\\n# Key Components:\\n# - Virtual Queue: Assigns queue positions and gradually admits users based on available capacity\\n# - Seat Map Service: Real-time seat availability backed by Redis bitmaps per event section\\n# - Hold Service: Temporarily locks selected seats for a user with a 5-10 minute expiration\\n# - Booking Service: Confirms purchase, charges payment, and issues e-tickets\\n# - Inventory Service: Redis-backed seat status (available/held/sold) with atomic transitions\\n#\\n# Architecture Notes:\\n# A virtual queue (waiting room) absorbs traffic spikes and meters users into the booking flow. A seat\\n`,
    explanation: `Use the virtual queue to flatten traffic spikes. Pre-load event inventory into Redis before on-sale. Shard by event ID so different events scale independently. Use WebSockets for real-time seat map updates. Separate read and write paths.\\n\\n\\n- Seat holds improve UX but reduce effective inventory while users browse; short timeouts frustrate users, long ones block others\\n- A strict FIFO queue is fair but slower to fill seats; allowing parallel access fills faster but feels less fair`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Design a Virtual Waiting Queue: Build a queue system that assigns each user a position when they arrive for a popular event. Admit users in batches (e.g., 1000 at a time) based on system capacity. Sho", "Implement Seat Hold with TTL: Create a system where selecting a seat places a temporary hold (5 minutes). If the user does not complete checkout, the hold expires and the seat becomes available again.", "Problem: Popular event on-sale crashes the system as millions of users flood in simultaneously within the first second. Solution: Implement a virtual waiting room that queues users before the booking ", "Problem: Users hold seats but abandon checkout, causing popular seats to appear unavailable for extended periods while nobody actually buys them. Solution: Set aggressive hold timeouts (5 minutes). Li"],
  },
  {
    id: 9015,
    description: `- Match customer orders with nearby restaurants and delivery drivers\\n- Provide real-time order tracking from placement to delivery\\n- Optimize delivery routes and estimated arrival times\\n- Handle dynamic pricing (surge) during peak demand\\n- Support restaurant menu management and order preparation workflows\\n`,
    examples: `Exercise 1: Design a Driver Matching Algorithm\\nBuild a dispatch system that assigns the best available driver to a new order. Consider: distance to restaurant, driver's current delivery status, estimated restaurant preparation time, and driver acceptance rate. Score and rank candidates.\\n\\nExercise 2: Implement Order State Machine\\nCreate an order lifecycle state machine with states: placed, restaurant_confirmed, preparing, ready_for_pickup, driver_assigned, picked_up, en_route, delivered, cancelled. Define valid transitions and the events that trigger them. Handle edge cases like cancellation at any stage.`,
    intuition: `A food delivery system is a real-time logistics platform that continuously solves a three-sided matching problem between customers, restaurants, and drivers while optimizing for speed, cost, and driver utilization. The geospatial matching and ETA prediction are the core algorithmic challenges.`,
    approach: `A customer-facing service handles browsing and ordering. An order service manages the order lifecycle as a state machine. A matching service assigns drivers to orders using location and optimization algorithms. A real-time tracking service uses WebSockets to push driver locations. A logistics engine optimizes batching and routing.\\n\\n\\n- **Order Service**: State machine managing order states (placed, confirmed, preparing, picked_up, en_route, delivered)\\n- **Matching/Dispatch Service**: Assigns drivers based on proximity, current load, and estimated pickup time using geospatial indexing\\n- **Location Service**: Ingests driver GPS updates (every 5 seconds) and stores in a geospatial index (Redis GEO or PostGIS)\\n- **ETA Service**: Predicts preparation time, pickup time, and delivery time using historical data and real-time traffic\\n- **Surge Pricing Engine**: Adjusts delivery fees based on supply (available drivers) and demand (order volume) per zone\\n`,
    code: `# System Design: Design a Food Delivery System (DoorDash)\\n#\\n# Key Components:\\n# - Order Service: State machine managing order states (placed, confirmed, preparing, picked_up, en_route, delivered)\\n# - Matching/Dispatch Service: Assigns drivers based on proximity, current load, and estimated pickup time using geospatial indexing\\n# - Location Service: Ingests driver GPS updates (every 5 seconds) and stores in a geospatial index (Redis GEO or PostGIS)\\n# - ETA Service: Predicts preparation time, pickup time, and delivery time using historical data and real-time traffic\\n# - Surge Pricing Engine: Adjusts delivery fees based on supply (available drivers) and demand (order volume) per zone\\n#\\n# Architecture Notes:\\n# A customer-facing service handles browsing and ordering. An order service manages the order lifecycl\\n`,
    explanation: `Partition geospatially by delivery zone. Use a geospatial index for efficient nearest-driver queries. Batch nearby orders for the same driver to improve efficiency. Use event sourcing for the order lifecycle to enable replay and analytics.\\n\\n\\n- Immediate dispatch reduces wait time but may not find the optimal driver; batching orders improves efficiency but adds delay\\n- Surge pricing balances supply and demand but frustrates customers; subsidizing drivers during peaks is costly`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Design a Driver Matching Algorithm: Build a dispatch system that assigns the best available driver to a new order. Consider: distance to restaurant, driver's current delivery status, estimated restaur", "Implement Order State Machine: Create an order lifecycle state machine with states: placed, restaurant_confirmed, preparing, ready_for_pickup, driver_assigned, picked_up, en_route, delivered, cancelle", "Problem: During dinner rush, there are not enough drivers and delivery times spike to over an hour, causing order cancellations. Solution: Implement surge pricing to incentivize more drivers to work d", "Problem: ETA estimates are consistently inaccurate, showing 30 minutes when actual delivery takes 50+ minutes, frustrating customers. Solution: Build an ML-based ETA model trained on historical delive"],
  },
  {
    id: 9016,
    description: `- Return top-k suggestions as user types each character\\n- Suggestions ranked by popularity, freshness, and personalization\\n- Latency under 100ms per keystroke; support billions of queries/day\\n- Handle multi-language input and trending queries in near real-time\\n`,
    examples: `Exercise 1: Build a Basic Trie with Top-K\\nImplement a trie that stores the top-5 suggestions at each node. Insert a corpus of 10,000 queries with frequencies and benchmark prefix lookup latency.\\n\\nExercise 2: Simulate Trending Query Injection\\nCreate a stream simulator that emits 'trending' queries at high frequency. Merge these into your trie in near real-time and verify that suggestions update within 5 seconds.`,
    intuition: `Search autocomplete is fundamentally a prefix-lookup problem where pre-computing the top results at every trie node turns an expensive traversal into an O(1) read. The real challenge is keeping those pre-computed results fresh as query popularity shifts in real time.`,
    approach: `A Trie (prefix tree) serves as the core data structure, stored in-memory across distributed nodes. An offline pipeline aggregates query logs, computes frequency scores, and rebuilds the trie periodically. A real-time layer captures trending queries via stream processing and merges them into the trie.\\n\\n\\n- **Trie Nodes**: Store top-k results at each prefix to avoid traversal at query time\\n- **Data Collection Service**: Aggregates raw query logs into frequency tables\\n- **Ranking Service**: Blends popularity, recency, and user context into a final score\\n- **Zookeeper/Consistent Hashing**: Partition tries by prefix range across nodes\\n`,
    code: `# System Design: Design Search Autocomplete (Google)\\n#\\n# Key Components:\\n# - Trie Nodes: Store top-k results at each prefix to avoid traversal at query time\\n# - Data Collection Service: Aggregates raw query logs into frequency tables\\n# - Ranking Service: Blends popularity, recency, and user context into a final score\\n# - Zookeeper/Consistent Hashing: Partition tries by prefix range across nodes\\n#\\n# Architecture Notes:\\n# A Trie (prefix tree) serves as the core data structure, stored in-memory across distributed nodes. A\\n`,
    explanation: `Shard tries alphabetically or by hash prefix. Use read replicas and CDN edge caching for hot prefixes. Replicate across regions for low latency.\\n\\n\\n- Pre-computed top-k per node trades storage for speed\\n- Periodic rebuild vs. real-time update: freshness vs. complexity\\n- Personalization increases relevance but adds privacy and storage cost`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Basic Trie with Top-K: Implement a trie that stores the top-5 suggestions at each node. Insert a corpus of 10,000 queries with frequencies and benchmark prefix lookup latency.", "Simulate Trending Query Injection: Create a stream simulator that emits 'trending' queries at high frequency. Merge these into your trie in near real-time and verify that suggestions update within 5 s", "Problem: Stale suggestions persist for hours after a trending event ends Solution: Implement a TTL or exponential decay on trending scores so entries naturally expire, and run incremental trie updates", "Problem: Memory usage spikes when the full trie is loaded on every node Solution: Partition the trie by prefix ranges using consistent hashing and route queries to the responsible shard."],
  },
  {
    id: 9017,
    description: `- Schedule one-time and recurring tasks (cron-like) across a cluster\\n- Guarantee at-least-once execution with distributed locking\\n- Support priorities, retries with backoff, and dead-letter handling\\n- Scale to millions of scheduled tasks with sub-second trigger accuracy\\n`,
    examples: `Exercise 1: Build a Time-Bucketed Dispatcher\\nImplement a dispatcher that partitions upcoming tasks into 1-second buckets, scans the current bucket, and dispatches tasks to worker threads. Measure trigger accuracy.\\n\\nExercise 2: Add Retry with Exponential Backoff\\nExtend your scheduler so that failed tasks are retried up to 3 times with exponential backoff (1s, 2s, 4s). After max retries, move to a dead-letter store.`,
    intuition: `A distributed task scheduler is essentially a time-indexed work queue with distributed coordination to ensure every task fires exactly when it should without duplicates. The hardest part is making the dispatcher layer itself fault-tolerant so that no scheduled task is ever lost or double-fired.`,
    approach: `Tasks are persisted in a durable store (e.g., MySQL or Cassandra) partitioned by scheduled time. A dispatcher layer polls for due tasks, acquires distributed locks, and dispatches work to a pool of executors. A heartbeat mechanism detects executor failures and re-queues tasks.\\n\\n\\n- **Task Store**: Time-partitioned table holding task metadata, schedule, retries\\n- **Dispatcher**: Leader-elected or sharded process that scans for due tasks\\n- **Executor Pool**: Stateless workers that run task payloads; report success/failure\\n- **Dead-Letter Queue**: Captures tasks that exceed max retries for investigation\\n`,
    code: `# System Design: Design a Distributed Task Scheduler\\n#\\n# Key Components:\\n# - Task Store: Time-partitioned table holding task metadata, schedule, retries\\n# - Dispatcher: Leader-elected or sharded process that scans for due tasks\\n# - Executor Pool: Stateless workers that run task payloads; report success/failure\\n# - Dead-Letter Queue: Captures tasks that exceed max retries for investigation\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition the timeline into buckets; assign each bucket to a dispatcher shard. Use consistent hashing to rebalance when dispatchers join or leave. Executors auto-scale based on queue depth.\\n\\n\\n- At-least-once is simpler but requires idempotent task handlers\\n- Polling vs. push: polling is simpler but adds latency; push via message queue is more responsive\\n- Storing tasks in a relational DB gives strong consistency; NoSQL gives better write throughput`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Time-Bucketed Dispatcher: Implement a dispatcher that partitions upcoming tasks into 1-second buckets, scans the current bucket, and dispatches tasks to worker threads. Measure trigger accurac", "Add Retry with Exponential Backoff: Extend your scheduler so that failed tasks are retried up to 3 times with exponential backoff (1s, 2s, 4s). After max retries, move to a dead-letter store.", "Problem: Tasks are triggered twice when a dispatcher fails over mid-execution Solution: Use fencing tokens: the executor checks that its token is still valid before committing results; stale tokens ar", "Problem: Hot time buckets cause dispatcher lag during bulk-scheduled events Solution: Sub-partition hot buckets by task ID hash and spread them across multiple dispatcher shards."],
  },
  {
    id: 9018,
    description: `- Crawl billions of web pages, respecting robots.txt and politeness policies\\n- Detect and handle duplicate content, redirect chains, and spider traps\\n- Prioritize pages by importance (PageRank, freshness, domain authority)\\n- Store raw HTML and extracted metadata for downstream indexing\\n`,
    examples: `Exercise 1: Implement a Polite URL Frontier\\nBuild a URL frontier that maintains per-domain back-queues with configurable crawl-delay. Verify that no domain is hit more than once per its specified delay.\\n\\nExercise 2: Near-Duplicate Detection with SimHash\\nImplement SimHash fingerprinting on a set of 1,000 HTML documents. Detect pages with >90% similarity and mark them as duplicates.`,
    intuition: `A web crawler is a massive producer-consumer system where the URL frontier is the central coordination point, balancing discovery speed against politeness constraints. The key insight is that per-host queuing and rate limiting are not optional niceties but fundamental architectural requirements.`,
    approach: `A URL Frontier holds URLs to crawl, ordered by priority and politeness constraints. Fetcher workers pull URLs, download pages, and pass HTML to a processing pipeline that extracts links, deduplicates content, and feeds new URLs back to the frontier. A central DNS resolver cache reduces lookup latency.\\n\\n\\n- **URL Frontier**: Priority queue with per-domain rate limiting (back queue per host)\\n- **Fetcher Workers**: Async HTTP clients that respect crawl-delay directives\\n- **Content Deduplicator**: SimHash or MinHash fingerprint to detect near-duplicates\\n- **Blob Store**: S3 or HDFS for raw HTML; metadata goes to a search index pipeline\\n`,
    code: `# System Design: Design a Web Crawler (Google)\\n#\\n# Key Components:\\n# - URL Frontier: Priority queue with per-domain rate limiting (back queue per host)\\n# - Fetcher Workers: Async HTTP clients that respect crawl-delay directives\\n# - Content Deduplicator: SimHash or MinHash fingerprint to detect near-duplicates\\n# - Blob Store: S3 or HDFS for raw HTML; metadata goes to a search index pipeline\\n#\\n# Architecture Notes:\\n# A URL Frontier holds URLs to crawl, ordered by priority and politeness constraints. Fetcher workers \\n`,
    explanation: `Partition the frontier by domain hash. Run fetcher workers across regions close to target servers. Use consistent hashing to ensure one domain is crawled by one worker (politeness).\\n\\n\\n- Breadth-first discovers more domains; best-first finds important pages faster\\n- Aggressive crawling gets fresher data but risks IP bans\\n- Storing all raw HTML is expensive but enables re-processing without re-crawling`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement a Polite URL Frontier: Build a URL frontier that maintains per-domain back-queues with configurable crawl-delay. Verify that no domain is hit more than once per its specified delay.", "Near-Duplicate Detection with SimHash: Implement SimHash fingerprinting on a set of 1,000 HTML documents. Detect pages with >90% similarity and mark them as duplicates.", "Problem: Crawler gets stuck in a spider trap generating infinite URLs Solution: Limit crawl depth per domain, normalize URLs by stripping known tracking parameters, and cap the number of URLs stored p", "Problem: DNS resolution becomes a bottleneck slowing the entire crawl Solution: Run a local DNS cache (e.g., dnsmasq) and batch DNS pre-resolution for queued URLs ahead of fetch time."],
  },
  {
    id: 9019,
    description: `- Serve static and dynamic content from edge locations closest to users\\n- Support cache invalidation, TTL-based expiry, and origin shielding\\n- Handle TLS termination, DDoS mitigation, and HTTP/2+ at the edge\\n- Deliver 99.99% availability with sub-50ms latency for cached content\\n`,
    examples: `Exercise 1: Simulate Tiered Caching\\nBuild a simulation with 5 edge caches, 1 shield cache, and 1 origin. Generate a Zipfian request distribution and measure hit rates at each tier as you vary TTL.\\n\\nExercise 2: Implement Cache Invalidation via Purge Propagation\\nDesign a purge system where the control plane issues an invalidation that reaches all edge nodes within 5 seconds. Test with 10 simulated edge nodes.`,
    intuition: `A CDN is a hierarchical caching system where the fundamental trade-off is freshness vs. latency: the closer content is cached to the user, the faster it is served, but the harder it is to keep current. The shield tier is the key architectural insight that collapses thousands of edge misses into a single origin request.`,
    approach: `A global network of PoPs (Points of Presence) each running reverse proxy/cache servers. DNS-based or Anycast routing directs users to the nearest PoP. On cache miss, the edge fetches from a mid-tier shield cache before hitting the origin, reducing origin load. A control plane pushes configuration and invalidation commands.\\n\\n\\n- **Edge Cache Servers**: Nginx/Varnish nodes with SSD-backed LRU caches\\n- **Shield/Mid-Tier Cache**: Aggregates cache misses from multiple edges to protect origin\\n- **DNS/Anycast Router**: Directs users to the optimal PoP based on latency and health\\n- **Control Plane**: Distributes config, purge commands, and TLS certificates\\n`,
    code: `# System Design: Design a Content Delivery Network (CDN)\\n#\\n# Key Components:\\n# - Edge Cache Servers: Nginx/Varnish nodes with SSD-backed LRU caches\\n# - Shield/Mid-Tier Cache: Aggregates cache misses from multiple edges to protect origin\\n# - DNS/Anycast Router: Directs users to the optimal PoP based on latency and health\\n# - Control Plane: Distributes config, purge commands, and TLS certificates\\n#\\n# Architecture Notes:\\n# A global network of PoPs (Points of Presence) each running reverse proxy/cache servers. DNS-based or\\n`,
    explanation: `Add PoPs in underserved regions. Use tiered caching (edge -> shield -> origin) to absorb traffic spikes. Horizontally scale edge nodes within a PoP behind a load balancer.\\n\\n\\n- Push vs. pull caching: push guarantees freshness but wastes bandwidth on unpopular content\\n- Long TTLs improve hit rate but delay content updates\\n- Anycast simplifies routing but complicates debugging and failover`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Simulate Tiered Caching: Build a simulation with 5 edge caches, 1 shield cache, and 1 origin. Generate a Zipfian request distribution and measure hit rates at each tier as you vary TTL.", "Implement Cache Invalidation via Purge Propagation: Design a purge system where the control plane issues an invalidation that reaches all edge nodes within 5 seconds. Test with 10 simulated edge nodes", "Problem: Origin server overwhelmed after a mass cache purge Solution: Use request coalescing at the shield tier: only one request goes to origin while others wait, and implement stale-while-revalidate", "Problem: Users in a specific region get stale content indefinitely Solution: Add health checks and config version verification in the control plane; flag PoPs running stale configurations and force a "],
  },
  {
    id: 9020,
    description: `- Ingest millions of time-series data points per second from agents on every host\\n- Support multi-dimensional tagging (host, service, region, custom labels)\\n- Provide real-time dashboards, alerting with thresholds/anomaly detection\\n- Retain high-resolution data for 15 days, downsampled data for 1+ year\\n`,
    examples: `Exercise 1: Build a Simple TSDB with Rollups\\nImplement an in-memory time-series store that accepts (metric, tags, timestamp, value) tuples. Add a background job that rolls up data older than 1 hour into 1-minute averages.\\n\\nExercise 2: Implement a Threshold Alert Evaluator\\nBuild an evaluator that checks a metric every 10 seconds and fires an alert if the value exceeds a threshold for 3 consecutive evaluation windows.`,
    intuition: `A monitoring system is a specialized streaming pipeline where every metric is a time-series append, and the core tension is between retaining high-resolution data for accurate alerting and compressing it for cost-effective long-term storage. Tagging is what makes the system powerful, but tag cardinality is what makes it expensive.`,
    approach: `Agents on each host collect and batch metrics, sending them to a regional intake API. The intake writes to a stream (Kafka) consumed by a storage writer and an alerting evaluator in parallel. Storage uses a purpose-built time-series database with columnar compression. A query engine serves dashboard and alert queries.\\n\\n\\n- **Agent**: Collects system and application metrics, pre-aggregates, batches sends\\n- **Intake/Router**: Validates, tags, and routes metrics to Kafka topics by type\\n- **TSDB Storage**: Write-optimized columnar store (e.g., InfluxDB, custom engine) with rollup compaction\\n- **Alert Evaluator**: Continuously evaluates alert rules against the stream; fires notifications\\n`,
    code: `# System Design: Design a Metrics/Monitoring System (Datadog)\\n#\\n# Key Components:\\n# - Agent: Collects system and application metrics, pre-aggregates, batches sends\\n# - Intake/Router: Validates, tags, and routes metrics to Kafka topics by type\\n# - TSDB Storage: Write-optimized columnar store (e.g., InfluxDB, custom engine) with rollup compaction\\n# - Alert Evaluator: Continuously evaluates alert rules against the stream; fires notifications\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition by metric name + tag combination. Use rollup/downsampling jobs to compress old data. Separate read and write paths so dashboard queries don't impact ingestion.\\n\\n\\n- Pre-aggregation at the agent reduces volume but loses granularity\\n- Pull-based collection (Prometheus) vs. push-based: pull is simpler to debug; push handles ephemeral hosts better\\n- Exact alerting on raw data is expensive; evaluating on rolled-up data may miss brief spikes`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Simple TSDB with Rollups: Implement an in-memory time-series store that accepts (metric, tags, timestamp, value) tuples. Add a background job that rolls up data older than 1 hour into 1-minute", "Implement a Threshold Alert Evaluator: Build an evaluator that checks a metric every 10 seconds and fires an alert if the value exceeds a threshold for 3 consecutive evaluation windows.", "Problem: High-cardinality tags (e.g., user_id) cause the TSDB to run out of memory Solution: Enforce a cardinality limit per metric, reject or drop tags exceeding the limit, and educate users to use l", "Problem: Alert evaluator falls behind during ingestion spikes, causing delayed alerts Solution: Give the alert evaluator its own consumer group with higher priority partitioning, and allow it to evalu"],
  },
  {
    id: 9021,
    description: `- Store billions of nodes (users) and hundreds of billions of edges (connections)\\n- Support queries: mutual friends, degrees of separation, friend recommendations\\n- Handle asymmetric (follow) and symmetric (friend) relationship types\\n- Serve graph traversal queries within 100ms for up to 3 hops\\n`,
    examples: `Exercise 1: Implement Bidirectional BFS for Mutual Friends\\nBuild a graph with 100,000 nodes and random edges. Implement bidirectional BFS to find mutual friends between two users and compare performance against naive single-source BFS.\\n\\nExercise 2: Build a Friend-of-Friend Recommendation Engine\\nGiven a user, compute the top-10 friend-of-friend suggestions ranked by number of mutual connections. Filter out existing friends.`,
    intuition: `A social graph system is an adjacency-list store where the fundamental challenge is that graph traversals touch many shards in a distributed setting, making multi-hop queries expensive. The practical solution is to precompute the most common queries offline and cache aggressively for high-degree nodes.`,
    approach: `An adjacency list representation stored in a distributed key-value or graph database, partitioned by user ID. A graph query engine executes BFS/DFS traversals, with caching for hot nodes (celebrities). An offline pipeline computes friend-of-friend recommendations and preloads them.\\n\\n\\n- **Graph Store**: Adjacency lists in TAO-like system (Facebook) or a graph DB (Neo4j/JanusGraph)\\n- **Cache Layer**: In-memory cache of adjacency lists for high-degree nodes\\n- **Traversal Engine**: Executes bounded BFS for mutual friends, shortest path queries\\n- **Recommendation Pipeline**: Offline MapReduce/Spark jobs compute PYMK (People You May Know)\\n`,
    code: `# System Design: Design a Social Graph (Facebook/LinkedIn)\\n#\\n# Key Components:\\n# - Graph Store: Adjacency lists in TAO-like system (Facebook) or a graph DB (Neo4j/JanusGraph)\\n# - Cache Layer: In-memory cache of adjacency lists for high-degree nodes\\n# - Traversal Engine: Executes bounded BFS for mutual friends, shortest path queries\\n# - Recommendation Pipeline: Offline MapReduce/Spark jobs compute PYMK (People You May Know)\\n#\\n# Architecture Notes:\\n# An adjacency list representation stored in a distributed key-value or graph database, partitioned by\\n`,
    explanation: `Partition by user ID with replication. Co-locate friends' adjacency lists when possible to reduce cross-shard hops. Use bidirectional BFS for shortest path to reduce search space exponentially.\\n\\n\\n- Graph databases are natural but harder to scale; key-value stores scale but require application-level traversal\\n- Precomputing recommendations is stale but fast; real-time traversal is fresh but expensive\\n- Storing reverse edges doubles storage but is essential for efficient fan-in queries`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Bidirectional BFS for Mutual Friends: Build a graph with 100,000 nodes and random edges. Implement bidirectional BFS to find mutual friends between two users and compare performance against ", "Build a Friend-of-Friend Recommendation Engine: Given a user, compute the top-10 friend-of-friend suggestions ranked by number of mutual connections. Filter out existing friends.", "Problem: Graph traversal queries timeout for celebrity accounts with millions of followers Solution: Cap the traversal fan-out by sampling edges for high-degree nodes, and serve precomputed results fo", "Problem: Cross-shard graph queries have high latency due to network round trips Solution: Use graph-aware partitioning (e.g., METIS) to co-locate densely connected subgraphs on the same shard, reducin"],
  },
  {
    id: 9022,
    description: `- Match riders with nearby drivers in real time (under 5 seconds)\\n- Track live driver locations with updates every 3-5 seconds\\n- Handle surge pricing based on supply/demand ratios per zone\\n- Support trip lifecycle: request, match, pickup, in-progress, complete, payment\\n`,
    examples: `Exercise 1: Build a Geohash-Based Nearest Driver Finder\\nIndex 10,000 simulated driver locations using geohashes. Given a rider location, find the 5 nearest available drivers by expanding geohash rings. Measure query latency.\\n\\nExercise 2: Implement a Simple Surge Pricing Model\\nDivide a city into a grid of zones. Track rider requests and available drivers per zone. Compute a surge multiplier when demand/supply ratio exceeds 1.5x.`,
    intuition: `A ride-sharing system is a real-time geospatial matching problem where the core challenge is maintaining a constantly-updating spatial index of drivers and querying it fast enough to match riders before drivers move significantly. The cell-based architecture ensures that a failure in one city never cascades to another.`,
    approach: `Drivers continuously report GPS locations to a Location Service that indexes them in a geospatial data structure. When a rider requests a trip, the Matching Service queries nearby available drivers, ranks them, and dispatches. A Trip Service manages the state machine for each ride. A separate Pricing Service computes dynamic fares.\\n\\n\\n- **Location Service**: Geohash or QuadTree index of active driver positions in Redis/memory\\n- **Matching Service**: Finds k-nearest available drivers, scores by ETA, rating, direction\\n- **Trip Service**: State machine (requested -> matched -> en_route -> in_trip -> completed)\\n- **Pricing Service**: Computes base fare + surge multiplier based on zone demand/supply ratio\\n`,
    code: `# System Design: Design a Ride-Sharing Service (Uber)\\n#\\n# Key Components:\\n# - Location Service: Geohash or QuadTree index of active driver positions in Redis/memory\\n# - Matching Service: Finds k-nearest available drivers, scores by ETA, rating, direction\\n# - Trip Service: State machine (requested -> matched -> en_route -> in_trip -> completed)\\n# - Pricing Service: Computes base fare + surge multiplier based on zone demand/supply ratio\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition geographically by city or geohash prefix. Location updates are write-heavy; use in-memory stores. Matching is compute-heavy; scale independently. Use cell-based architecture for fault isolation per city.\\n\\n\\n- Geohash is simpler but creates edge-of-cell boundary issues; QuadTree adapts to density but is harder to distribute\\n- Greedy matching (nearest driver) is fast but globally suboptimal; batch matching optimizes overall wait time but adds latency\\n- Frequent location updates improve accuracy but increase bandwidth and processing cost`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Geohash-Based Nearest Driver Finder: Index 10,000 simulated driver locations using geohashes. Given a rider location, find the 5 nearest available drivers by expanding geohash rings. Measure q", "Implement a Simple Surge Pricing Model: Divide a city into a grid of zones. Track rider requests and available drivers per zone. Compute a surge multiplier when demand/supply ratio exceeds 1.5x.", "Problem: Riders at geohash cell boundaries get matched with far-away drivers while nearby ones in the adjacent cell are ignored Solution: Always search the target cell plus all adjacent cells (9-cell ", "Problem: Driver location updates are lost during peak hours causing stale matches Solution: Use a partitioned in-memory store (e.g., Redis Cluster) for location writes, and switch to TCP with backpres"],
  },
  {
    id: 9023,
    description: `- Provide a browser-based IDE with syntax highlighting, autocomplete, and file management\\n- Execute user code in isolated sandboxed environments per session\\n- Support multiple programming languages with language-specific toolchains\\n- Allow users to share and fork projects; persist workspace state\\n`,
    examples: `Exercise 1: Build a WebSocket-Based Terminal Relay\\nCreate a server that spawns a subprocess (e.g., Python REPL) per WebSocket connection, relays stdin/stdout between the browser and the process, and cleans up on disconnect.\\n\\nExercise 2: Implement Resource-Limited Code Execution\\nBuild an execution engine that runs user-submitted code with a 5-second timeout, 256MB memory limit, and no network access. Test with CPU bombs and fork bombs.`,
    intuition: `An online code editor is fundamentally a thin browser client connected to a per-user sandboxed compute environment, where the main challenge is provisioning and isolating thousands of concurrent sandboxes with low startup latency. Pre-warming container pools is the critical optimization that makes the experience feel instant.`,
    approach: `A web frontend delivers the editor UI (Monaco/CodeMirror). Each user session spins up an isolated container (Docker/microVM) with the appropriate language runtime. A WebSocket connection streams terminal I/O between browser and container. A workspace service persists files to object storage.\\n\\n\\n- **Editor Frontend**: Monaco editor in browser with LSP integration for autocomplete\\n- **Container Orchestrator**: Provisions, monitors, and recycles per-user sandboxes (Kubernetes/Firecracker)\\n- **Execution Engine**: Runs code inside the container, captures stdout/stderr, enforces resource limits\\n- **Workspace Storage**: Git-backed or object-store-backed file persistence per project\\n`,
    code: `# System Design: Design an Online Code Editor (Replit)\\n#\\n# Key Components:\\n# - Editor Frontend: Monaco editor in browser with LSP integration for autocomplete\\n# - Container Orchestrator: Provisions, monitors, and recycles per-user sandboxes (Kubernetes/Firecracker)\\n# - Execution Engine: Runs code inside the container, captures stdout/stderr, enforces resource limits\\n# - Workspace Storage: Git-backed or object-store-backed file persistence per project\\n#\\n# Architecture Notes:\\n# A web frontend delivers the editor UI (Monaco/CodeMirror). Each user session spins up an isolated co\\n`,
    explanation: `Pre-warm container pools per language to reduce cold start. Use Firecracker microVMs for fast boot (<150ms) and strong isolation. Auto-scale container hosts based on active session count.\\n\\n\\n- Containers are flexible but heavier; microVMs are lighter and more secure but less compatible\\n- Full LSP per session provides rich IDE features but is resource-intensive\\n- Persistent storage per project increases cost; ephemeral workspaces are cheaper but frustrate users`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a WebSocket-Based Terminal Relay: Create a server that spawns a subprocess (e.g., Python REPL) per WebSocket connection, relays stdin/stdout between the browser and the process, and cleans up on", "Implement Resource-Limited Code Execution: Build an execution engine that runs user-submitted code with a 5-second timeout, 256MB memory limit, and no network access. Test with CPU bombs and fork bomb", "Problem: Cold start takes 10+ seconds when a user selects an uncommon language Solution: Maintain a small pre-warmed pool for all supported languages, and use layered container images with a shared ba", "Problem: A malicious user escapes the container and accesses host resources Solution: Use rootless containers or Firecracker microVMs, apply seccomp profiles, drop all capabilities, and run with a rea"],
  },
  {
    id: 9024,
    description: `- Multiple users edit the same document simultaneously with <100ms perceived latency\\n- All users converge to the same document state regardless of edit order\\n- Support rich text formatting, comments, cursor presence, and revision history\\n- Handle offline edits that sync when connectivity is restored\\n`,
    examples: `Exercise 1: Implement Basic Operational Transformation\\nBuild an OT engine for a plain-text document supporting insert(pos, char) and delete(pos) operations. Write a transform function that correctly handles two concurrent inserts at different positions.\\n\\nExercise 2: Build a Collaborative Editing Server\\nCreate a WebSocket server that accepts operations from multiple clients, applies OT to resolve conflicts, and broadcasts the transformed operations to all connected clients.`,
    intuition: `Real-time collaboration is a distributed consensus problem disguised as a text editing feature: every client has a local copy and the system must ensure all copies converge to the same state despite concurrent edits arriving in different orders. OT achieves this by transforming operations against each other; CRDTs achieve it by making operations inherently commutative.`,
    approach: `Clients apply edits locally for instant feedback, then send operations to a collaboration server. The server uses Operational Transformation (OT) or a CRDT to merge concurrent edits deterministically. Transformed operations are broadcast to all other clients. A persistence layer stores the document and its full operation history.\\n\\n\\n- **Client Editor**: Applies local edits optimistically; queues operations for server sync\\n- **OT/CRDT Engine**: Transforms concurrent operations to preserve intent (OT) or uses conflict-free merge (CRDT)\\n- **Collaboration Server**: Serializes operations, assigns global order, broadcasts to clients\\n- **Document Store**: Persists document snapshots and operation logs for history/undo\\n`,
    code: `# System Design: Design a Real-Time Collaborative Editor (Google Docs)\\n#\\n# Key Components:\\n# - Client Editor: Applies local edits optimistically; queues operations for server sync\\n# - OT/CRDT Engine: Transforms concurrent operations to preserve intent (OT) or uses conflict-free merge (CRDT)\\n# - Collaboration Server: Serializes operations, assigns global order, broadcasts to clients\\n# - Document Store: Persists document snapshots and operation logs for history/undo\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition by document: each document is handled by one server (sticky sessions). Use sharding across millions of documents. Take periodic snapshots to truncate the operation log.\\n\\n\\n- OT is well-proven (Google Docs) but requires a central server for ordering; CRDTs enable P2P but are more complex and memory-heavy\\n- Storing every operation enables full revision history but grows storage linearly\\n- Optimistic local apply provides instant UX but requires complex conflict resolution`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Basic Operational Transformation: Build an OT engine for a plain-text document supporting insert(pos, char) and delete(pos) operations. Write a transform function that correctly handles two ", "Build a Collaborative Editing Server: Create a WebSocket server that accepts operations from multiple clients, applies OT to resolve conflicts, and broadcasts the transformed operations to all connect", "Problem: Documents diverge after a rapid sequence of concurrent edits from 3+ users Solution: Ensure the server imposes a total order on operations: each client must acknowledge the server's version b", "Problem: Operation log grows unboundedly for long-lived documents, slowing down new client joins Solution: Periodically snapshot the document state and truncate the log. New clients load the latest sn"],
  },
  {
    id: 9025,
    description: `- Publish and consume millions of messages per second with low latency\\n- Guarantee ordering within a partition and at-least-once delivery\\n- Support consumer groups, offset management, and message replay\\n- Retain messages for configurable durations (hours to weeks)\\n`,
    examples: `Exercise 1: Implement a Partition Log with Segment Rotation\\nBuild an append-only log that writes to segment files. When a segment exceeds 1MB, rotate to a new segment. Support reading from any offset across segments.\\n\\nExercise 2: Build a Consumer Group Rebalancer\\nImplement partition assignment for a consumer group: when consumers join or leave, reassign partitions evenly. Test with 12 partitions and dynamically adding/removing consumers.`,
    intuition: `A distributed message queue is an append-only distributed log where the key insight is that sequential disk I/O (appending) is fast enough to rival network throughput, eliminating the need for complex in-memory caching. Partitions are the unit of parallelism, ordering, and fault tolerance all at once.`,
    approach: `Producers send messages to a cluster of brokers. Each topic is split into partitions, distributed across brokers. Messages within a partition are appended to an immutable, sequential log. Consumers in a group are each assigned a subset of partitions and track their offset. Replication across brokers provides fault tolerance.\\n\\n\\n- **Broker**: Stores partition log segments on disk, serves produce/consume requests\\n- **Partition Log**: Append-only file with sequential offsets; zero-copy reads via sendfile()\\n- **Consumer Group Coordinator**: Assigns partitions to consumers, manages rebalancing\\n- **Replication Manager**: Maintains in-sync replicas (ISR) with leader-follower protocol\\n`,
    code: `# System Design: Design a Distributed Message Queue (Kafka)\\n#\\n# Key Components:\\n# - Broker: Stores partition log segments on disk, serves produce/consume requests\\n# - Partition Log: Append-only file with sequential offsets; zero-copy reads via sendfile()\\n# - Consumer Group Coordinator: Assigns partitions to consumers, manages rebalancing\\n# - Replication Manager: Maintains in-sync replicas (ISR) with leader-follower protocol\\n#\\n# Architecture Notes:\\n`,
    explanation: `Add partitions to increase parallelism; add brokers and rebalance partitions. Producers partition by key hash for ordering guarantees. Use tiered storage to offload old segments to S3.\\n\\n\\n- More partitions increase parallelism but add rebalancing overhead and metadata cost\\n- At-least-once is default; exactly-once requires idempotent producers and transactional consumers\\n- Long retention enables replay but increases storage cost; tiered storage mitigates this`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement a Partition Log with Segment Rotation: Build an append-only log that writes to segment files. When a segment exceeds 1MB, rotate to a new segment. Support reading from any offset across segm", "Build a Consumer Group Rebalancer: Implement partition assignment for a consumer group: when consumers join or leave, reassign partitions evenly. Test with 12 partitions and dynamically adding/removin", "Problem: Consumer rebalances cause processing pauses of 30+ seconds during deployments Solution: Use incremental cooperative rebalancing: only revoke partitions that are moving, allowing consumers to ", "Problem: A slow consumer causes the entire consumer group to lag behind Solution: Monitor per-consumer lag and reassign partitions from slow consumers. Consider splitting the slow consumer's partition"],
  },
  {
    id: 9026,
    description: `- Send and receive emails supporting SMTP, IMAP, and POP3 protocols\\n- Store billions of emails with full-text search across subject, body, and attachments\\n- Provide spam filtering, labels/folders, threaded conversations, and attachment handling\\n- Support push notifications for new mail with sub-second delivery\\n`,
    examples: `Exercise 1: Build a Threaded Conversation Grouper\\nGiven a set of emails with In-Reply-To and References headers, group them into conversation threads. Handle cases where intermediate emails are missing.\\n\\nExercise 2: Implement a Simple Spam Classifier\\nBuild a Naive Bayes spam classifier trained on a labeled email dataset. Compute precision and recall on a test set. Integrate it as a filter step in a mock email pipeline.`,
    intuition: `An email service is a per-user document store with a complex ingestion pipeline, where the fundamental design insight is that emails are written once and read many times, so optimizing the read path (search, threading, labeling) matters more than write throughput. Spam filtering is where most of the engineering complexity actually lives.`,
    approach: `Incoming mail arrives via SMTP servers, passes through a spam/virus filter pipeline, and is stored in a distributed mail store indexed per user. A search service indexes email content using an inverted index. Outgoing mail goes through a sending pipeline with DKIM/SPF signing. A notification service pushes new-mail events to connected clients.\\n\\n\\n- **SMTP Gateway**: Receives inbound email, performs MX lookup for outbound routing\\n- **Spam Filter Pipeline**: Multi-stage filter (SPF/DKIM check, ML classifier, rule engine)\\n- **Mail Store**: Bigtable-like storage with per-user partitioning and message deduplication\\n- **Search Index**: Per-user inverted index updated asynchronously on mail receipt\\n`,
    code: `# System Design: Design an Email Service (Gmail)\\n#\\n# Key Components:\\n# - SMTP Gateway: Receives inbound email, performs MX lookup for outbound routing\\n# - Spam Filter Pipeline: Multi-stage filter (SPF/DKIM check, ML classifier, rule engine)\\n# - Mail Store: Bigtable-like storage with per-user partitioning and message deduplication\\n# - Search Index: Per-user inverted index updated asynchronously on mail receipt\\n#\\n# Architecture Notes:\\n`,
    explanation: `Partition by user ID; each user's mailbox lives on one shard with replication. Blob storage for large attachments (referenced by pointer in the mail store). Separate the search index into its own cluster.\\n\\n\\n- Per-user partitioning simplifies access patterns but creates hot shards for heavy users\\n- Synchronous spam filtering adds latency to delivery; async risks delivering spam first\\n- Full-text search of attachments requires OCR/parsing pipelines, adding complexity`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Threaded Conversation Grouper: Given a set of emails with In-Reply-To and References headers, group them into conversation threads. Handle cases where intermediate emails are missing.", "Implement a Simple Spam Classifier: Build a Naive Bayes spam classifier trained on a labeled email dataset. Compute precision and recall on a test set. Integrate it as a filter step in a mock email pi", "Problem: Search results are missing recently received emails Solution: Implement a real-time indexing path for recent emails (last N minutes) that queries the mail store directly, merged with the asyn", "Problem: Sending reputation degrades, causing outbound emails to land in recipients' spam folders Solution: Implement outbound rate limits per user, detect anomalous sending patterns, and use dedicate"],
  },
  {
    id: 9027,
    description: `- Ingest terabytes of log data daily from thousands of services and hosts\\n- Support structured and unstructured log formats with schema evolution\\n- Provide near real-time search (under 30 seconds from emission to searchable)\\n- Retain logs for 30-90 days with archival to cold storage for compliance\\n`,
    examples: `Exercise 1: Build a Log Pipeline with Backpressure\\nCreate a pipeline: log generator -> in-memory buffer -> batch writer to disk. Implement backpressure so the generator slows down when the buffer is 80% full.\\n\\nExercise 2: Implement Time-Based Index Rotation\\nSimulate an indexing system that creates a new index daily, automatically deletes indices older than 7 days, and redirects all writes to the current day's index.`,
    intuition: `A logging pipeline is a high-throughput ETL system where the key insight is that logs have a natural time dimension that dictates everything: partitioning, retention, and query patterns. The collection tier (Kafka) is the shock absorber that prevents bursty log producers from overwhelming the indexing layer.`,
    approach: `Log agents on each host tail log files and ship events to a collection tier (Kafka/Fluentd). A stream processing layer enriches, parses, and routes logs by type. Hot logs go to an indexing cluster (Elasticsearch/OpenSearch) for search. Cold logs are archived to S3/GCS in columnar format (Parquet) for batch analytics.\\n\\n\\n- **Log Agent**: Filebeat/Fluentd on each host; handles backpressure and local buffering\\n- **Collection Tier**: Kafka topics partitioned by service; decouples producers from consumers\\n- **Stream Processor**: Flink/Logstash that parses, enriches (adds host/region tags), and routes\\n- **Index Cluster**: Elasticsearch with time-based indices and ILM (Index Lifecycle Management) for rollover and deletion\\n`,
    code: `# System Design: Design a Logging/Analytics Pipeline\\n#\\n# Key Components:\\n# - Log Agent: Filebeat/Fluentd on each host; handles backpressure and local buffering\\n# - Collection Tier: Kafka topics partitioned by service; decouples producers from consumers\\n# - Stream Processor: Flink/Logstash that parses, enriches (adds host/region tags), and routes\\n# - Index Cluster: Elasticsearch with time-based indices and ILM (Index Lifecycle Management) for rollover and deletion\\n#\\n# Architecture Notes:\\n`,
    explanation: `Time-based index partitioning (one index per day) for easy retention management. Scale Kafka partitions and ES data nodes independently. Use sampling for very high-volume debug logs.\\n\\n\\n- Indexing everything enables ad-hoc search but is expensive; selective indexing saves cost but requires knowing what to keep upfront\\n- Shorter index retention reduces cost but limits incident investigation windows\\n- Structured logging is easier to query but requires upfront schema discipline from developers`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a Log Pipeline with Backpressure: Create a pipeline: log generator -> in-memory buffer -> batch writer to disk. Implement backpressure so the generator slows down when the buffer is 80% full.", "Implement Time-Based Index Rotation: Simulate an indexing system that creates a new index daily, automatically deletes indices older than 7 days, and redirects all writes to the current day's index.", "Problem: Elasticsearch cluster goes red during log bursts from a single service Solution: Implement per-service rate limiting in the stream processor, and use separate Kafka topics or ES indices for h", "Problem: Log searches return incomplete results for recent events Solution: Reduce the refresh interval to 5 seconds for hot indices (accepting higher resource usage), or provide a separate real-time "],
  },
  {
    id: 9028,
    description: `- Generate personalized recommendations for millions of users across items (products, content, people)\\n- Combine collaborative filtering, content-based, and popularity signals\\n- Serve recommendations with <100ms latency; refresh daily with near real-time boosts\\n- Handle cold-start for new users and new items\\n`,
    examples: `Exercise 1: Implement Matrix Factorization with ALS\\nBuild a basic Alternating Least Squares recommender on the MovieLens 100K dataset. Split into train/test, compute RMSE, and generate top-10 recommendations for a user.\\n\\nExercise 2: Build a Two-Stage Recommend-and-Rank Pipeline\\nImplement candidate generation using item-item similarity (cosine on feature vectors) and a ranking stage that re-orders candidates by a weighted score combining similarity, recency, and popularity.`,
    intuition: `A recommendation engine is a two-stage system: a broad candidate generation phase that retrieves hundreds of plausible items, followed by a precise ranking phase that scores and orders them using richer features. The cold-start problem is solved not by one technique but by gracefully falling back through a hierarchy of signals: collaborative, content-based, then popularity.`,
    approach: `An offline pipeline trains models on user-item interaction data (views, clicks, purchases) using collaborative filtering (matrix factorization, ALS) and content features. Pre-computed recommendations per user are stored in a serving layer. An online layer re-ranks results using real-time signals (session context, recency) before serving.\\n\\n\\n- **Feature Store**: Stores user profiles, item features, interaction history for training and serving\\n- **Training Pipeline**: Spark/TensorFlow jobs that train CF and content-based models on interaction matrices\\n- **Candidate Generation**: Retrieves top-N candidates per user from pre-computed lists or ANN (approximate nearest neighbor) index\\n- **Ranking Service**: Re-ranks candidates with a lightweight model using real-time features\\n`,
    code: `# System Design: Design a Recommendation Engine\\n#\\n# Key Components:\\n# - Feature Store: Stores user profiles, item features, interaction history for training and serving\\n# - Training Pipeline: Spark/TensorFlow jobs that train CF and content-based models on interaction matrices\\n# - Candidate Generation: Retrieves top-N candidates per user from pre-computed lists or ANN (approximate nearest neighbor) index\\n# - Ranking Service: Re-ranks candidates with a lightweight model using real-time features\\n#\\n# Architecture Notes:\\n# An offline pipeline trains models on user-item interaction data (views, clicks, purchases) using col\\n`,
    explanation: `Pre-compute and cache recommendations per user. Use ANN indexes (FAISS, ScaNN) for real-time candidate retrieval from embedding space. Separate candidate generation (recall) from ranking (precision) to manage compute.\\n\\n\\n- Collaborative filtering is powerful but suffers cold-start; content-based handles new items but lacks serendipity\\n- Pre-computed recs are fast but stale; real-time models are fresh but expensive\\n- Optimizing for engagement may create filter bubbles; diversity injection hurts click-through but improves satisfaction`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Matrix Factorization with ALS: Build a basic Alternating Least Squares recommender on the MovieLens 100K dataset. Split into train/test, compute RMSE, and generate top-10 recommendations for", "Build a Two-Stage Recommend-and-Rank Pipeline: Implement candidate generation using item-item similarity (cosine on feature vectors) and a ranking stage that re-orders candidates by a weighted score c", "Problem: Recommendations become stale and repetitive for active users Solution: Add a real-time boosting layer that promotes items similar to the user's current session activity, and penalize already-", "Problem: New items never get recommended because they lack interaction data Solution: Use content-based features (title, category, description embeddings) to place new items in the embedding space, an"],
  },
  {
    id: 9029,
    description: `- Trigger builds on push, pull request, schedule, or manual dispatch\\n- Execute multi-step workflows with parallel and sequential jobs on isolated runners\\n- Support artifact caching, secret management, and environment-specific deployments\\n- Scale to thousands of concurrent builds with job queuing and prioritization\\n`,
    examples: `Exercise 1: Build a DAG-Based Workflow Executor\\nImplement a workflow engine that parses a YAML-defined job DAG (with 'needs' dependencies), runs independent jobs in parallel, and respects sequential dependencies. Track pass/fail status.\\n\\nExercise 2: Implement Dependency Caching for Builds\\nCreate a cache layer keyed by a hash of lock files (e.g., package-lock.json). On cache hit, restore the dependency directory; on miss, run install and upload the result.`,
    intuition: `A CI/CD pipeline is a distributed job execution engine triggered by source control events, where the central challenge is balancing the throughput of thousands of concurrent builds against the isolation guarantees each build needs. Ephemeral runners provide the cleanest isolation but make caching the critical optimization to avoid rebuilding the world every time.`,
    approach: `A webhook listener receives events from the source control system and enqueues workflow runs. A scheduler matches jobs to available runners from a pool of ephemeral VMs/containers. Each runner executes steps in an isolated environment, streaming logs back to the control plane. An artifact store holds build outputs and caches.\\n\\n\\n- **Webhook/Event Listener**: Receives SCM events, evaluates trigger conditions, creates workflow runs\\n- **Scheduler**: Matches pending jobs to runners by label (OS, architecture), manages queue priority\\n- **Runner Pool**: Ephemeral VMs or containers provisioned on demand; cleaned up after each job\\n- **Artifact & Cache Store**: S3-backed storage for build artifacts, dependency caches, and logs\\n`,
    code: `# System Design: Design a CI/CD Pipeline (GitHub Actions)\\n#\\n# Key Components:\\n# - Webhook/Event Listener: Receives SCM events, evaluates trigger conditions, creates workflow runs\\n# - Scheduler: Matches pending jobs to runners by label (OS, architecture), manages queue priority\\n# - Runner Pool: Ephemeral VMs or containers provisioned on demand; cleaned up after each job\\n# - Artifact & Cache Store: S3-backed storage for build artifacts, dependency caches, and logs\\n#\\n# Architecture Notes:\\n# A webhook listener receives events from the source control system and enqueues workflow runs. A sche\\n`,
    explanation: `Auto-scale runner pools based on queue depth. Use spot/preemptible instances for cost savings with checkpointing for interrupted jobs. Cache dependency downloads aggressively to reduce build times.\\n\\n\\n- Ephemeral runners are clean and secure but have cold-start overhead; persistent runners are faster but risk contamination\\n- Shared caches speed up builds but introduce cache invalidation complexity\\n- Self-hosted runners give control but require maintenance; managed runners are convenient but limit customization`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Build a DAG-Based Workflow Executor: Implement a workflow engine that parses a YAML-defined job DAG (with 'needs' dependencies), runs independent jobs in parallel, and respects sequential dependencies", "Implement Dependency Caching for Builds: Create a cache layer keyed by a hash of lock files (e.g., package-lock.json). On cache hit, restore the dependency directory; on miss, run install and upload t", "Problem: Builds queue for 10+ minutes during peak hours despite available cloud capacity Solution: Implement predictive scaling based on historical patterns (e.g., post-standup push spikes), and reduc", "Problem: A secret is exposed in build logs, creating a security incident Solution: Implement automatic secret masking in the log streaming layer: scan output for known secret values and replace them w"],
  },
  {
    id: 9030,
    description: `- Serve thousands of tenants (organizations) from a shared infrastructure\\n- Isolate tenant data, enforce per-tenant resource quotas, and prevent noisy-neighbor effects\\n- Support tenant-specific customization (branding, feature flags, integrations)\\n- Provide tenant onboarding, billing metering, and admin dashboards\\n`,
    examples: `Exercise 1: Implement Row-Level Tenant Isolation\\nCreate a shared PostgreSQL database with a tenant_id column on all tables. Implement row-level security policies so that queries from tenant A can never see tenant B's data, even with SQL injection.\\n\\nExercise 2: Build a Per-Tenant Rate Limiter\\nImplement a token bucket rate limiter that enforces different rate limits per tenant (e.g., free: 100 req/min, pro: 1000 req/min). Test with concurrent requests from multiple tenants.`,
    intuition: `Multi-tenancy is the art of sharing infrastructure across customers while making each customer feel like they have their own dedicated system. The core architectural decision is the data isolation model, which exists on a spectrum from fully shared (cheap, risky) to fully isolated (expensive, safe), and most real systems offer tiered options.`,
    approach: `A shared application tier routes requests by tenant ID (extracted from subdomain or JWT). A data tier supports multiple isolation models: shared database with tenant_id column, schema-per-tenant, or database-per-tenant for premium tiers. A control plane manages tenant configuration, feature flags, and quota enforcement.\\n\\n\\n- **Tenant Router**: Extracts tenant context from request, applies rate limits, routes to appropriate resources\\n- **Data Isolation Layer**: Row-level security (RLS) in shared DB or separate schemas/databases per tenant\\n- **Quota/Rate Limiter**: Per-tenant rate limiting and resource quotas (API calls, storage, compute)\\n- **Billing Meter**: Tracks usage events per tenant for usage-based billing; emits to billing system\\n`,
    code: `# System Design: Design a Multi-Tenant SaaS Platform\\n#\\n# Key Components:\\n# - Tenant Router: Extracts tenant context from request, applies rate limits, routes to appropriate resources\\n# - Data Isolation Layer: Row-level security (RLS) in shared DB or separate schemas/databases per tenant\\n# - Quota/Rate Limiter: Per-tenant rate limiting and resource quotas (API calls, storage, compute)\\n# - Billing Meter: Tracks usage events per tenant for usage-based billing; emits to billing system\\n#\\n# Architecture Notes:\\n# A shared application tier routes requests by tenant ID (extracted from subdomain or JWT). A data tie\\n`,
    explanation: `Use shared infrastructure for small tenants (cost-efficient) and dedicated resources for large/enterprise tenants (performance isolation). Shard the shared database by tenant_id. Cache tenant configs aggressively.\\n\\n\\n- Shared DB is cost-efficient but risks data leaks via bugs; separate DBs are safer but expensive\\n- Uniform infrastructure simplifies ops but cannot meet diverse tenant SLAs; tiered offerings add complexity\\n- Tenant customization increases flexibility but multiplies testing surface area`,
    timeComplexity: "N/A - System Design",
    spaceComplexity: "N/A - System Design",
    hints: ["Implement Row-Level Tenant Isolation: Create a shared PostgreSQL database with a tenant_id column on all tables. Implement row-level security policies so that queries from tenant A can never see tenan", "Build a Per-Tenant Rate Limiter: Implement a token bucket rate limiter that enforces different rate limits per tenant (e.g., free: 100 req/min, pro: 1000 req/min). Test with concurrent requests from m", "Problem: A large tenant's heavy queries slow down the shared database for all other tenants Solution: Implement per-tenant connection pool limits, add query timeouts, and migrate large tenants to dedi", "Problem: A developer accidentally deploys a migration that exposes tenant A's data to tenant B Solution: Enforce row-level security at the database level (not just application level) as a safety net, "],
  },
];
